module desugar

imports

  BaseC/trans/desugar/-
  MetaC/sm/trans/desugar

rules

  desugar01 =
    fail

  desugar(|t) =
    fail

  desugar =
    fail

  desugar-all(|t) =
    bottomup(
      try(desugar01)
      ; try(desugar(|t))
      ; try(desugar)
      ; try(flatten-list)
   )
