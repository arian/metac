module generate-declarations

language Metac

start symbol Program

test function [[
  int32 ** a(int8 **x) {
    return x;
  }
]] build generate-c to "signed int** a(signed char** x) {
  return x;
}"

test function with array parameter [[
  int32 a(int16 a[]) {
    return a[1];
  }
]] build generate-c to "signed int a(signed int a[]) {
  return a[1];
}"
