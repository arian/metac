module metac-runtime-utils/string

strategies

  join-strings(|sep): [] -> ""
  join-strings(|sep): list -> <map(\ x -> [sep, x] \)
    ; concat
    ; Tl
    ; concat-strings> list

  join-strings = join-strings(|" ")
